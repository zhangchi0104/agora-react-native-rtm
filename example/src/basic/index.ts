import Login from './Login';
import CreateStreamChannel from './createStreamChannel';

const Basic = {
  title: 'Basic',
  data: [
    {
      name: 'Login',
      component: Login,
    },
    {
      name: 'CreateStreamChannel',
      component: CreateStreamChannel,
    },
  ],
};
export default Basic;
